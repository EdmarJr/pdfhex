package com.bufferteknoloji.extract.b;

import java.awt.event.MouseAdapter;
import java.awt.event.MouseEvent;
import javax.swing.JPopupMenu;

final class d
  extends MouseAdapter
{
  private b paramb;

d(b paramb) {
	this.paramb = paramb;}
  
  public final void mouseReleased(MouseEvent paramMouseEvent)
  {
//    paramb.show(paramMouseEvent.getComponent(), paramMouseEvent.getX(), paramMouseEvent.getY());
//    b.e(this.a).show(paramMouseEvent.getComponent(), paramMouseEvent.getX(), paramMouseEvent.getY());
  }
}
